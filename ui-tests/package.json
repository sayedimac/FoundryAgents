{
  "name": "githubagent-ui-tests",
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install"
  }
}
