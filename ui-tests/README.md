# UI tests (Playwright)

From repo root:

- Install: `cd ui-tests; npm install; npm run install:browsers`
- Run: `npm test`

Optional overrides:

- `PLAYWRIGHT_BASE_URL` (default `https://127.0.0.1:5173`)
- `WEBAPP_HTTPS_PORT` / `WEBAPP_HTTP_PORT`
